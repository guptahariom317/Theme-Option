# Theme-Option
Theme Option for Custom Wordpress Theme

Place "truethemes_framework" folder into your active theme and include following code into the function file.

/*Include the theme options */
include TEMPLATEPATH. '/truethemes_framework/truethemes_framework_init.php';
